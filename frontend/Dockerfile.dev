# Dockerfile.dev
FROM node:18-alpine

WORKDIR /app

# We don't need COPY commands for dependencies or source code
# as they will be mounted through volumes

# Expose Vite dev server port
EXPOSE 5173

# Start development server
CMD ["npm", "run", "dev"]
