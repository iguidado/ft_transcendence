export default {
	polar: {
		rotateX: Math.PI/2,
		rotateY: -Math.PI/4, 
		phi: Math.PI/18,
		theta: -Math.PI/4,
		useCalculatedRadius: true,
		calculatedRadiusMargin: 1.3
	},
	fov: 70,
	lookAt: new THREE.Vector3(0, 0, 0)
}