import * as THREE from "three"

export default {
	boundingBoxMargin: 20,
	polar: {
		rotateX: Math.PI/2,
		rotateY: -Math.PI/4, 
		phi: Math.PI/18,
		theta: -Math.PI/4,
		useCalculatedRadius: true,
		calculatedRadiusMargin: 1.3
	}
}
